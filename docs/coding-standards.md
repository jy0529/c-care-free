# 编码规范

## 命名

- 类型、属性、方法：PascalCase
- 局部变量、参数：camelCase
- 接口：`I` 前缀
- 异步方法：`Async` 后缀

## 分层规则

- Domain 不依赖 UI 或基础设施实现
- Application 依赖抽象接口
- Infrastructure 提供实现
- App 不直连文件系统

## 注释要求

- 公共接口与重要服务写 XML 注释
- 复杂清理逻辑写简短说明
- 不写机械型注释

## 异常与日志

- 禁止吞异常不记录
- 用户提示与诊断日志分离
